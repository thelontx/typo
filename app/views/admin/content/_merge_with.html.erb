    <h4><%= _("Merge Articles")%></h4>
    
<%= form_tag(controller: "admin/content", action: "merge_articles", :id => @article.id) do %>
  <%= label_tag(:merge_with, "Article ID") %>
  <%= text_field_tag(:merge_with) %>
  <%= submit_tag("Merge") %>

<% end %>
